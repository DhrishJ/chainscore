# dependencies
node_modules/

# next.js build output
.next/
out/

# environment variables — NEVER commit these
.env
.env.local
.env.*.local

# vercel
.vercel

# ML training data (can be large)
ml/data/

# ML training pipeline — venv, fetched data, parquets, reports (never deploy/commit)
model/

# misc
.DS_Store
*.pem
npm-debug.log*
tsconfig.tsbuildinfo

/lib/generated/prisma
